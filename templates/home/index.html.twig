{% extends 'base.html.twig' %}

{% block title %}Homepage {% endblock %}

{% block body %}


    {% for advertisement in advertisements if advertisements is defined %}
        {{ advertisement.title }}
        {{ advertisement.description }}
        {{ advertisement.User.username }}
        {{ advertisement.date|date("Y-m-d") }}
    {% endfor %}
{% endblock %}